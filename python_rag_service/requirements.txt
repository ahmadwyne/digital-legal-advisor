# Digital Legal Advisor RAG Service -- Python dependencies
# Install with: pip install -r requirements.txt
#
# For BACKEND=local (GGUF model), also run:
#   pip install llama-cpp-python
# (not listed here because it needs platform-specific compilation or pre-built wheel)

fastapi>=0.100.0
uvicorn[standard]>=0.23.0
groq>=0.11.0
rank-bm25>=0.2.2
transformers>=4.30.0
torch>=2.0.0
faiss-cpu>=1.7.4
numpy>=1.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
